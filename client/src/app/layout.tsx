import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Stellar Racer — Interstellar Arcade on Soroban",
  description:
    "Pilot your starship through the asteroid belt at lightspeed. Set high scores, compete on the global leaderboard, and earn on-chain rewards — all on the Stellar Soroban blockchain.",
  openGraph: {
    title: "Stellar Racer",
    description: "A cosmic dodge game powered by Stellar Soroban",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
